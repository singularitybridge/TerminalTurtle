# TerminalTurtle Environment Configuration

NODE_ENV=development
PORT=8080
WORKING_DIRECTORY=/data/workspace
AGENT_NAME=terminal-turtle

# Ngrok Configuration
# Set ENABLE_NGROK to 'true' to enable ngrok tunneling, defaults to 'false' if not set
ENABLE_NGROK=false
# Your ngrok auth token (only required if ENABLE_NGROK=true)
NGROK_AUTHTOKEN=
